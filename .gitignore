# Generated data artifacts — regenerable from staging sources, never committed
catalog/**/*.parquet
catalog/**/*.pmtiles

# Downloaded source data
staging/*/data/
# National census downloads (ACS .dat, LODES gz, CB zips) — a 1.4 GB cache
# that tools/fetch_census.py re-downloads on demand
staging/census-cache/

# Secrets
.env

# OS / editor noise
.DS_Store
__pycache__/
*.pyc
